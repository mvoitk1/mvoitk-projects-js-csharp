@model WebApp.ViewModels.HomeViewModel
@{
    ViewData["Title"] = "Home";
}

<!-- Hero -->
<div class="home-hero mt-4">
    <span class="section-eyebrow">New Season</span>
    <h1>Style for the<br>modern wardrobe</h1>
    <p>Explore our latest collections — thoughtfully crafted, endlessly wearable.</p>
    <a asp-controller="Shop" asp-action="Index" class="btn btn-dark btn-lg">Shop Now</a>
</div>

<!-- Collections -->
@if (Model.Collections.Any())
{
    <section class="mb-5">
        <div class="section-header">
            <h2 class="section-heading">Collections</h2>
        </div>
        <div class="row g-3">
            @foreach (var collection in Model.Collections.Take(3))
            {
                <div class="col-md-4">
                    <a asp-controller="Shop" asp-action="Index" asp-route-collectionId="@collection.Id"
                       class="collection-card">
                        <p class="section-eyebrow">Collection</p>
                        <h3 class="collection-card-title">@collection.Name</h3>
                        @if (!string.IsNullOrEmpty(collection.Description))
                        {
                            <p class="collection-card-desc">@collection.Description</p>
                        }
                        <span class="btn btn-dark btn-sm">View Collection</span>
                    </a>
                </div>
            }
        </div>
    </section>
}

<!-- Featured Products -->
<section class="mb-5">
    <div class="section-header">
        <h2 class="section-heading">Featured Products</h2>
        <a asp-controller="Shop" asp-action="Index" class="text-decoration-none" style="font-size:0.8rem;letter-spacing:0.05em;text-transform:uppercase;color:var(--c-text-muted);">View all →</a>
    </div>
    <div class="row g-3 row-cols-2 row-cols-md-3 row-cols-lg-4">
        @foreach (var product in Model.FeaturedProducts)
        {
            <div class="col">
                <a asp-controller="Shop" asp-action="Detail" asp-route-id="@product.Id" class="product-card">
                    <div class="product-card-img-wrap">
                        @if (!string.IsNullOrEmpty(product.PrimaryImageUrl))
                        {
                            <img src="@product.PrimaryImageUrl" alt="@product.Name">
                        }
                        else
                        {
                            <div class="product-card-img-placeholder">No image</div>
                        }
                    </div>
                    <p class="product-card-name">@product.Name</p>
                    @if (product.LowestPrice.HasValue)
                    {
                        <p class="product-card-price">From @product.LowestPrice.Value.ToString("C")</p>
                    }
                </a>
            </div>
        }
    </div>
</section>

<!-- Categories -->
@if (Model.Categories.Any())
{
    <section class="mb-5">
        <div class="section-header">
            <h2 class="section-heading">Shop by Category</h2>
        </div>
        <div class="row g-3">
            @foreach (var category in Model.Categories.Take(4))
            {
                <div class="col-6 col-md-3">
                    <a asp-controller="Shop" asp-action="Index" asp-route-categoryId="@category.Id"
                       class="category-card">
                        <span class="category-card-name">@category.Name</span>
                    </a>
                </div>
            }
        </div>
    </section>
}
