stages:
  - deploy

deploy_javascript_a1:
  stage: deploy
  only:
    - main
  tags:
    - shared
  script:
    - cd JS/assignment1
    - docker compose -p jsassignment1 up --build --remove-orphans --detach