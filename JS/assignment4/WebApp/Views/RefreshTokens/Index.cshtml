@model IEnumerable<App.Domain.Identity.RefreshToken>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AppUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Token)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TokenExpirationDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreviousToken)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PreviousTokenExpirationDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AppUser!.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Token)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TokenExpirationDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreviousToken)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreviousTokenExpirationDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
